use "../Shared" as knowledge.
use ProtossNexus as knowledge.
use "../Modules/PerceptsInit".

module ProtossNexusInit {
	if true then PerceptsInit.
	
	if bel(self(Id, _), status(_, _, _, _, _, X1, Y1, RegionId), X2 is X1 - 2, Y2 is Y1 - 1)
		then (mapagent).send:(ownBase(Id, X2, Y2, RegionId)).
}